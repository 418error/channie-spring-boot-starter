# channie-spring-boot-starter
Example of a working custom spring boot starter using gradle.  
Publishes to local vanilla Nexus.  
Adds a new endpoint `/some-message` which changes depending on the application properties set
```yaml
channie:
  message: nice
```